version: '3'

services:
  nginx-proxy:
    image: jwilder/nginx-proxy:alpine
    networks:
      - nginx-net
    ports:
      - "8000:80"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
    restart: unless-stopped

networks:
  nginx-net:
    external:
      name: nginx-net

